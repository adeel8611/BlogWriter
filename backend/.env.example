# Database
DB_HOST=localhost
DB_PORT=5432
DB_NAME=blogwriter
DB_USER=postgres
DB_PASSWORD=your_password

# Anthropic Claude API
ANTHROPIC_API_KEY=your_anthropic_api_key

# Publishing API (mock for now)
PUBLISHING_API_URL=https://api.example.com/publish
PUBLISHING_API_KEY=your_publishing_api_key

# Server
PORT=3001

# Vector DB (using PostgreSQL with pgvector for MVP)
# Alternative: Use Pinecone/Weaviate
VECTOR_DB_TYPE=pgvector
